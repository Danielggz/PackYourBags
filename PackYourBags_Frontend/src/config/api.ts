//LOCALHOST api base options local or cloud
const LOCAL = "http://localhost:8080";

//Set ip (varies) from cloud env
//const cloudIp = "";
//const CLOUD = `http:/${cloudIp}/:8080`;

export const API_BASE_URL = LOCAL;